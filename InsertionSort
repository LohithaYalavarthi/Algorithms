var arr1 = [1,3,7,5,8] ;

Take arr1 and have variable i and var j 
compare the 1st pos to 2nd pos if second pos is greater than first pos 

copy first into second array and second to first 
increment i++ and j++ 

take third array and compare with the remaining two elements j



function InsertionSort(arr1){
	var key=0;
    for(var i=1 ;i < arr1.length; i++){
      key = arr1[i]
      var j= i-1;

     while(j>=0 && arr1[j] > key ){
      
         arr1[j+1] = arr1[j];
         j = j-1;
       
      }
       arr1[j+1] = key;
     console.log(arr1)

    }
} 

InsertionSort([1,4,3,5,2])